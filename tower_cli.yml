to do:

1.)create file 

/etc/yum.repos.d/tower-cli.repo 

with

[tower-cli]
baseurl = https://releases.ansible.com/ansible-tower-cli/rpm/epel-7-x86_64/
sslverify = 0
name = Ansible Tower-cli
enabled = 1
gpgcheck = 0

2.) run

yum install ansible-tower-cli

3.) optionally copy you ~/.tower-cli.cfg

